"""V1 Search Router Tests — REMOVED.

The V1 POST /search endpoint and its router (app/routers/search.py) were
removed in Phase V2-2.2 as part of the V2 batch pipeline migration.

The V2 batch router (app/routers/batch.py) replaces all V1 search
functionality. Batch API tests live in:
    tests/test_v2_integration.py (TestBatchAPIIntegration)
    tests/routers/test_batch.py

This file is intentionally empty to preserve the routers/ directory structure.
"""
