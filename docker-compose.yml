version: "3.9"

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "BreachSearch2024!"
    ports:
      - "1434:1433"

  app:
    build: .
    depends_on:
      - sqlserver
    env_file: .env
    volumes:
      - ./data:/app/data
